export const metadata = {
  title: "Dashboard | AI Chatbot Builder",
  description:
    "Manage chatbots, monitor conversations, and track user engagement.",
};

export default function DashboardLayoutWrapper({ children }) {
  return children;
}
